name: Firebase Cron

on:
  schedule:
    - cron: "*/15 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Call Firebase Function
        run: |
          curl -X GET "https://asia-northeast1-kariya-bus.cloudfunctions.net/fetchBusData"
